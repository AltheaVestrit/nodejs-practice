// A few cool methods that are available through the global object

// THE GLOBAL OBJECT
// console.log(global);

// SET TIMEOUT AND SET INTERVAL
// setTimeout() is a shorthand for global.setTimeout()
// setTimeout(() => {
//     console.log('In the timeout');
//     clearInterval(int);
// }, 3000);

// const int = setInterval(() => {
//     console.log('In the interval');
// }, 1000);

// DIRECTORY AND FILE NAME
// console.log(__dirname);
// console.log(__filename);